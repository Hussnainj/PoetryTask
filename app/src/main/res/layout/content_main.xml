<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.wasim.poetrytask.MainActivity"
    tools:showIn="@layout/activity_main"
    android:background="@color/accent_material_light">

    <ImageView
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:id="@+id/imageView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="37dp"
        android:src="@drawable/poetryicon"
       />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View Poetry"
        android:id="@+id/viewBtn"
        android:layout_below="@+id/imageView"
        android:layout_marginTop="63dp"
        android:layout_alignRight="@+id/imageView"
        android:layout_alignEnd="@+id/imageView"
        android:layout_alignLeft="@+id/imageView"
        android:layout_alignStart="@+id/imageView"
        android:onClick="ViewData"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter New Poetry"
        android:id="@+id/addBtn"
        android:layout_below="@+id/viewBtn"
        android:layout_marginTop="61dp"
        android:layout_alignRight="@+id/viewBtn"
        android:layout_alignEnd="@+id/viewBtn"
        android:layout_alignLeft="@+id/viewBtn"
        android:layout_alignStart="@+id/viewBtn"
        android:onClick="movedata"/>
</RelativeLayout>
